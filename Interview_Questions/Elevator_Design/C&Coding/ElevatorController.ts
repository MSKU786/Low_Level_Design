import { Elevator } from './Elevator';
import { Direction } from './enum';

export class ElevatorController {
  elevator: Elevator;
  acceptRequest(floor: number, direction: Direction) {}

  controlECar() {}
}
