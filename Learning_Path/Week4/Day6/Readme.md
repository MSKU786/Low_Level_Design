Structural Patterns Combined Challenge

Combined Challenge:
Design a CMS that stores, renders and delivers content. This combines Coposite (content tree). Decorator(content processing pipeline), Proxy(access control + caching), Adapter(external storage), Facade(simple API), and Bridge(context \* output format)
